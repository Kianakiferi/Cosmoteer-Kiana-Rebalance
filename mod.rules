// 每个模组必须使用唯一标识并与其他模组不同的 ID
// 为了避免 ID 与其他模组冲突，您*必须*遵循“作者名.模组名”格式。
// *不要*使用“Cosmoteer”作为你的作者名字 —— 这是为官方模组所保留的!
ID = kianakiferi.cosmoteer_rebalance

// 您必须为您的模组指定一个易于阅读的名称。这将在用户界面中显示。
Name = "Cosmoteer Rebalance"

// 将在用户界面中显示的模组版本。可选。
Version = 0.1

// 此模组兼容的 Cosmoteer 版本列表。
// 如果这个模块启用，但它与 Cosmoteer 的版本不兼容
// 在游戏运行时，会显示警告。
// 在升级游戏时，不兼容新版本的模组也将被自动禁用。
CompatibleGameVersions = ["0.20.27"]

// 如果模组对游戏的更改可能影响多人游戏，则应将其设置为 TRUE
// 如果该模组实际上对游戏没有任何更改，在设置为 FALSE 时*或许*可以同其他没有此模组的玩家共同游戏。
// (将此设置为 FALSE 将*不会*与神奇般的允许您与没有该模组的玩家共同游戏；这样只会减少错误的信息量。
// 如果省略，则默认为 TRUE。
ModifiesMultiplayer = true

// 将在用户界面中显示的模组创建者们的名称。可选。
Author = "Kianakiferi"

// 将在用户界面中显示的模组创建者(们)的名称。可选。
Logo = "logo.png"

// 更详细地描述您的模组对游戏的影响。可选。
Description = "该模组重新平衡了部分游戏的数值\n\n"\
              "- 略微提高了船员的移动速度\n\n"\
              "- 提高资源堆叠数量\n\n"\
              "- 提高小行星矿物丰度\n\n"\
              "- 提高宿舍船员容量\n\n"\
              "- 降低开采激光能耗\n\n"\
              "- 降低传感器阵列人员需求"

// 模组可以定义每种语言的文本。
// 字符串文件 (比如 zh-cn.rules) 中的值将会添加并覆盖
// 基本字符串文件中的任何值 (比如 Data\strings\en.rules).
// 每种支持的语言都应该有自己的字符串文件，
// 以该语言的两个字母 ISO 639-1 语言标准编码命名。
StringsFolder = "strings"

// 模组修改游戏的主要方式是 “Actions”
//
// Actions 是在游戏的 .rules 文件加载到内存后修改它们的小命令，
// (实际中，在 'Data' 中的文件不会被修改，
// 因此禁用模组会使游戏恢复到一般状态。)
//
// 首先，游戏正常加载普通的 .rules 文件。
// 对于每个启用的模组，该模组的 “Actions” 
// 是在加载的 .rules 文件之上运行，并对其进行修改。
//
// 使用这项技术，模组几乎可以任何改动游戏。
// 直接修改 .rules 文件可以实现定制，但非永久性更改游戏文件更佳。
//
// 游戏文件夹中的示例展示了在原生 .rules 数据上执行的所有不同类型的“Actions”。
//
// 请注意，修改游戏会很容易导致加载时游戏崩溃。
// Log.txt 文件可能包含有关崩溃原因的详细信息。

// 有少量 .ruls 文件无法使用操作进行修改。
// 最突出的例子为上方的使用 StringsFolder 设置项修改的语言字符串文件(如 en.rules)。
Actions: &<crew/crew.rules>/Actions		// 提高船员移速
&<resources/resources.rules>/Actions	// 提高资源堆叠数量，提高小行星矿物丰度
&<ships/terran/ships.rules>/Actions		// 提高宿舍船员容量，降低开采激光能耗，降低传感器阵列人员需求
[

]